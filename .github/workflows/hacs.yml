name: Validate

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions: {}

jobs:
  validate-hacs:
    runs-on: "ubuntu-24.04-arm"
    steps:
      - name: HACS validation
        uses: "hacs/action@main"
        with:
          category: "integration"
